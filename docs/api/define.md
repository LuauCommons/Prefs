# Define

```lua
local Preferences = Prefs.define(schema, { name = "PlayerPreferences" })
```

`schema` is a map of setting names to builders. `name` is optional, defaults to `"Prefs"`, and identifies the networking namespace.
