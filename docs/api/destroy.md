# Destroy

```lua
Preferences:Destroy()
```

`Destroy` is server-only. It disconnects runtime listeners, cleans store state, removes networking created by that runtime, and prevents further operations. Treat a definition as lasting for the game session in this beta.
